name: offsignal_core
description: Pure Dart LT fountain-code transport, framing, and integrity layer for OffSignal.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  archive: ^4.0.7
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
